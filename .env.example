# Telegram bot
BOT_TOKEN=
AUTHORIZED_USER_ID=
ALLOWED_CHAT_ID=

# General
TZ=Europe/Warsaw
# Host folder mounted into containers at DOWNLOADS_PATH.
DOWNLOADS_HOST_PATH=./downloads
DOWNLOADS_PATH=/downloads
PLEX_SERVICE_NAME=plexmediaserver
PUID=1000
PGID=1000

# qBittorrent
QB_URL=http://vpn:8080
QB_USER=
QB_PASS=
QB_WEBUI_PORT=8080
QB_TORRENT_PORT=6881
# Internal Gluetun control API used by /status. Do not expose port 8000 publicly.
VPN_CONTROL_URL=http://vpn:8000
# URL used by qBittorrent completion hooks from inside the qBittorrent container.
QB_AUTORUN_QB_URL=http://127.0.0.1:8080
QB_CATEGORY_MOVIE_PATH=/downloads/Movie
QB_CATEGORY_TV_PATH=/downloads/TV
QB_CATEGORY_OTHERS_PATH=/downloads/Others

# Prowlarr, preferred for the new version
PROWLARR_URL=http://prowlarr:9696
PROWLARR_API_KEY=
# Torznab path segment used as: ${PROWLARR_URL}/${PROWLARR_DEFAULT_INDEXER}/api
PROWLARR_DEFAULT_INDEXER=all
PROWLARR_PORT=9696

# Optional Cloudflare helper for selected Prowlarr indexers. Start it with:
# docker compose --profile cloudflare up -d
# Configure Prowlarr's Indexer Proxy URL as http://flaresolverr:8191.
# Do not publish port 8191 to the host.
FLARESOLVERR_LOG_LEVEL=info

# Jackett, optional legacy compatibility
JACKETT_URL=http://jackett:9117
JACKETT_API_KEY=
JACKETT_DEFAULT_INDEXER=all
JACKETT_PORT=9117
# Optional temporary migration fallback for old JSON config.
JACKETT_CONFIG_PATH=config.sample.json

# qBittorrent post-download hook options
NOTIFY_CHAT_ID=
QBT_DELETE_DELAY=15

# VPN placeholders for qmcgaw/gluetun. Exact values depend on provider/protocol.
# Proton speed note: WireGuard usually performs better than OpenVPN.
VPN_PROVIDER=
VPN_TYPE=openvpn
VPN_USERNAME=
VPN_PASSWORD=
VPN_WIREGUARD_PRIVATE_KEY=
VPN_WIREGUARD_ADDRESSES=
VPN_WIREGUARD_MTU=
VPN_SERVER_COUNTRIES=
VPN_FIREWALL_OUTBOUND_SUBNETS=
# Proton/qBittorrent port forwarding. Enable NAT-PMP when generating the Proton
# WireGuard config, then enable these and recreate the vpn/qbittorrent services.
VPN_PORT_FORWARDING=off
VPN_PORT_FORWARDING_PROVIDER=
VPN_PORT_FORWARDING_STATUS_FILE=/tmp/gluetun/forwarded_port
# Optional: sync Gluetun's forwarded port into qBittorrent. Requires qBittorrent
# Web UI to bypass authentication for localhost. The explicit 10.2.0.2 binding
# matches the Address in Proton's usual manual WireGuard config; change both the
# WireGuard address and command if Proton generated a different tunnel address.
VPN_PORT_FORWARDING_UP_COMMAND=/bin/sh -c 'wget -qO- --retry-connrefused --post-data "json={\"listen_port\":{{PORT}},\"current_network_interface\":\"{{VPN_INTERFACE}}\",\"current_interface_address\":\"10.2.0.2\",\"random_port\":false,\"upnp\":false}" http://127.0.0.1:8080/api/v2/app/setPreferences'
VPN_PORT_FORWARDING_DOWN_COMMAND=/bin/sh -c 'wget -qO- --retry-connrefused --post-data "json={\"listen_port\":0,\"current_network_interface\":\"lo\"}" http://127.0.0.1:8080/api/v2/app/setPreferences'

# Watchtower
WATCHTOWER_CLEANUP=true
WATCHTOWER_POLL_INTERVAL=86400
